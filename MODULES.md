# 项目模块说明

本文档说明 `src/` 下已有模块与新增 A1 上层算法模块的职责。团队自研模块统一放在 `src/a1/` 下；该目录只用于组织代码，不应创建 `package.xml`。各子目录未来仍建议使用带 `a1_` 前缀的 ROS 包名，例如物理目录 `src/a1/navigation` 对应 ROS 包名 `a1_navigation`。

## 现有环境与底层模块

### `building_generator_core`

**定位与职责：** 与具体仿真平台相对独立的建筑生成核心，负责建筑约束、楼层、房间、走廊、家具、门、电梯及布局数据的生成和基础导出；上层导航团队通常只消费其生成结果，不应在此放置导航或感知算法。

### `building_generator_classic`

**定位与职责：** 面向 Gazebo Classic 的建筑导出和运行时控制模块，读取门与电梯配置，通过 Gazebo 服务实现动态门板、电梯轿厢及乘客模型控制，并向上层提供 `/set_door_state` 和 `/call_elevator` 等接口。

### `building_generator_interfaces`

**定位与职责：** 定义建筑控制相关的 ROS 消息和服务，是 `building_generator_classic` 与上层特殊行为模块之间的通信契约；已有接口应优先复用，避免在 A1 自研模块中重复定义门和电梯协议。

### `building_obstacles`

**定位与职责：** 负责比赛场景组装、危险源与干扰源生成、真值文件和公开场景信息输出，以及危险源结果评估；参赛算法只能使用公开接口，不应读取其中生成的裁判真值作为算法输入。

### `Mid360_imu_sim`

**定位与职责：** 提供 Livox Mid-360 激光雷达及其 IMU 的 Gazebo 仿真模型、扫描模式和点云插件，是 FAST-LIO2 的主要模拟传感器数据来源；定位团队负责适配其输出话题和时间戳。

### `unitree_guide`

**定位与职责：** 提供 Unitree A1 模型、关节控制器、状态机、RL 步态和 Gazebo ROS 接口，接收最终 `/cmd_vel` 并完成后续低层运动控制；上层团队原则上不修改 `State_RL` 及其后的控制链。

### `uav_simulator`

**定位与职责：** 保存项目已有的地图、感知、可视化和仿真工具组件，其中部分通用功能可供参考或复用，但它不是当前 `auto.sh` 启动 A1 比赛环境的核心调用链，复用前应确认接口和维护状态。

### `generated_building`

**定位与职责：** `src/` 下已有的生成场景副本或历史产物目录，不属于新的 A1 上层算法模块；正式运行时主要使用项目根目录的 `generated_building/`，不应在这里开发业务代码。

## `third_party`

**定位与职责：** 用于存放 FAST-LIO2 等外部开源依赖，原则上保持上游代码结构和行为，不在其中混入楼层管理、导航、比赛任务或危险源识别等业务逻辑，以便后续升级和替换第三方实现。

**验收标准：** 第三方组件能够独立编译和运行；来源、版本和必要补丁可追溯；移除项目业务代码后仍保持完整，不要求其他团队直接修改其内部实现。

## A1 上层算法模块

### `a1/navigation_interfaces`

**定位与职责：** 统一定义上层模块之间使用的自定义 ROS 消息、服务和 Action，例如楼层状态、导航状态、危险源检测结果及跨楼层行为接口，避免各团队重复或冲突地定义通信协议；建议 ROS 包名为 `a1_navigation_interfaces`。

**验收标准：** 接口定义能够通过 Catkin 编译并被其他包正确依赖；字段语义、单位、坐标系和错误状态明确；接口稳定且不存在功能重复的消息或服务。

### `a1/localization`

**定位与职责：** 负责 FAST-LIO2 与当前 Livox、IMU 和 A1 坐标系的适配，向其他模块提供连续的三维位姿、里程计、三维点云地图、注册点云、TF 和定位健康状态，不承担二维地图投影和路径规划；建议 ROS 包名为 `a1_localization`。

**验收标准：** 不使用 Gazebo 裁判真值也能持续输出有效位姿；TF 树连通且时间戳正确；机器人静止时漂移处于约定范围；定位异常或丢失时能明确报告状态。

### `a1/floor_mapping`

**定位与职责：** 根据三维定位和点云地图识别机器人当前楼层，并按当前楼层的有效高度范围完成地面过滤和三维到二维投影，为导航模块发布当前楼层的二维占据栅格及楼层状态。

**验收标准：** 能稳定识别当前楼层和楼层切换；二维地图与当前楼层墙体、门和通道基本一致；不会把其他楼层结构错误投影到当前层；输出符合 `nav_msgs/OccupancyGrid` 规范。

### `a1/navigation`

**定位与职责：** 负责基于 `move_base` 的单楼层二维导航，接收当前楼层地图、机器人位姿和导航目标，完成全局路径规划、局部避障和恢复行为，并将速度输出到 `/cmd_vel_nav`，不得直接占用最终 `/cmd_vel`。

**验收标准：** 能在单楼层内到达多个可达目标并避开障碍；不可达或失败时返回明确状态；输出速度频率稳定且满足 A1 限制；停止、取消或故障后不继续输出运动指令。

### `a1/exploration`

**定位与职责：** 负责在当前楼层二维地图上选择自主探索目标，例如检测和评价地图前沿、记录已访问或不可达区域，并持续向导航模块提供合适的下一目标，不直接生成关节控制或最终速度。

**验收标准：** 能持续生成有效且可达的探索目标；不会频繁重复失败目标；能识别当前楼层探索完成或无目标状态；探索过程能够覆盖约定比例的可通行区域。

### `a1/mission_manager`

**定位与职责：** 负责整个比赛任务的顶层状态机和模块调度，包括系统初始化、当前层探索、跨楼层决策、特殊行为调用、失败恢复、任务结束和结果保存通知，不在本模块内处理原始点云或图像算法。

**验收标准：** 状态转换条件清晰且可追踪；定位未就绪时不会启动导航；普通导航和跨楼层行为能够正确暂停、切换和恢复；模块失败时能进入安全状态；完整任务能够正常结束。

### `a1/building_behavior`

**定位与职责：** 负责门、楼梯和电梯等不能直接作为普通二维导航处理的特殊行为，调用已有 `/set_door_state` 与 `/call_elevator` 服务，并在必要时通过 `/cmd_vel_behavior` 控制机器人对准、进入、退出或通过特殊区域。

**验收标准：** 门、电梯服务失败时不会继续盲目运动；特殊行为支持成功、失败和取消结果；行为期间能够暂停普通导航；机器人可完成约定的进门、出门、乘梯或楼梯通行测试。

### `a1/cmd_mux`

**定位与职责：** 作为所有上层速度命令到 A1 底层的唯一出口，对导航、特殊行为、人工接管和紧急停止等速度源进行优先级仲裁、限速、加速度限制和超时保护，最终只由本模块发布 `/cmd_vel`。

**验收标准：** 任一输入超时后能在约定时间内输出零速度；高优先级输入能够可靠覆盖低优先级输入；速度和加速度不超过配置限制；模式切换无明显残留指令；任何其他新模块均不直接发布 `/cmd_vel`。

### `a1/danger_perception`

**定位与职责：** 负责利用 RealSense、点云和定位信息检测危险源、排除干扰物，并将检测目标转换到统一全局坐标系后发布带置信度的结构化结果，不直接写比赛最终 JSON 文件。

**验收标准：** 能区分规定的危险源与主要干扰物；输出坐标使用约定坐标系且误差满足比赛需求；不依赖裁判真值；对无效深度、定位丢失和重复观测有明确处理。

### `a1/result_manager`

**定位与职责：** 负责汇总危险源检测结果，执行置信度筛选、空间聚类、多帧去重和探索时间记录，并按比赛格式安全写入 `results/detected_danger.json`，使感知算法与最终文件格式解耦。

**验收标准：** 重复观测不会产生多个结果；低置信度或无效坐标能够被过滤；生成的 JSON 字段和格式通过评估脚本检查；任务中断或正常结束时不会产生损坏的半写文件。

### `a1/bringup`

**定位与职责：** 负责组合 FAST-LIO2、楼层地图、导航、探索、特殊行为、速度仲裁、危险源感知、任务管理和结果管理的启动文件与系统级配置，本目录只承担启动编排和健康检查，不放置具体算法实现。

**验收标准：** 能通过一个统一入口启动所需上层模块；启动顺序和依赖等待正确；话题、参数和 TF 重映射一致；关键节点缺失时能够给出清晰错误并阻止机器人危险运动。

### `a1/navigation_tests`

**定位与职责：** 用于存放跨模块集成测试、测试场景和调试工具，覆盖速度接口、定位、单层导航、楼层切换、门、电梯、楼梯、危险源识别和完整任务流程，避免临时测试脚本散落到业务模块。

**验收标准：** 关键模块接口均有可重复运行的测试；测试能够明确报告通过或失败；至少覆盖固定速度、单层目标导航、楼层切换和完整任务四类场景；测试失败时能保留必要日志以便定位问题。

## 总体集成约束

- `a1/localization` 统一提供三维定位、地图与 TF。
- `a1/floor_mapping` 统一提供当前楼层状态和二维地图。
- `a1/navigation` 仅负责当前楼层导航，并输出 `/cmd_vel_nav`。
- `a1/building_behavior` 输出 `/cmd_vel_behavior`，负责特殊区域通行。
- `a1/cmd_mux` 是唯一允许发布最终 `/cmd_vel` 的上层模块。
- `unitree_guide` 与 `State_RL` 继续作为现有底层控制使用，除非底层接口确有缺陷，否则上层团队不应修改。
